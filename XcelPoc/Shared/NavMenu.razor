<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <img src="/img/logo-trans.png" alt="RTA Logo" width="70px" height="50px" />
        <a class="navbar-brand" href="">MS Excel POC</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
      @*  <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>*@
        @*<div class="nav-item px-3">
            <NavLink class="nav-link" href="compareSheets">
                <span class="oi oi-plus" aria-hidden="true"></span> Compare Sheets
            </NavLink>
        </div>*@
        <div class="nav-item px-3">
            <NavLink class="nav-link"  href="CompareWorkbook">
                <span class="oi oi-list-rich" aria-hidden="true"></span>Compare Workbook
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
